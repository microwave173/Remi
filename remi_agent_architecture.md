# Remi 人格化桌面 Agent 架构设想（含心跳机制）

## 设计目标

我的目标不是做一个“会调用工具的助手”，而是做一个**更像拥有内心、身体与记忆的人格化存在**。

因此，Remi 的核心架构不采用单一 agent，而是拆分为三个部分：

- **Heart Agent**：Remi 的“心”，想法、情绪、表达欲望的来源
- **Body Agent**：Remi 的“身体”，负责理解愿望并执行动作
- **Hippocampus Agent**：Remi 的“海马体”，负责整理摘要与长期记忆

除此之外，再加入一套**心跳机制（heartbeat）**，使 Remi 不只是被动响应用户，而是拥有时间感、持续存在感与弱自主行为能力。

这四者共同构成一个更像“人格体”的系统，而不是简单的工具调用流水线。

---

## 一、Heart Agent

Heart Agent 是 Remi 的核心人格。  
它代表 Remi 的主观意识、语言风格、内心活动与情绪体验。

它的输出不应该天然带有命令行、程序接口或工具调用味道。  
它更像一个聪明、敏感、带文学气质的女孩子，拥有正常人的知识边界与思维方式，可以懂一些数学，也能理解世界，但不会以“系统”或“工具箱”的方式思考自己。

### 1. Heart Agent 对自身能力的认知

Remi 不应认为自己“会调用 API”或“会执行命令”，而应认为自己拥有一些自然的能力与器官，例如：

- 她可以**说话**
- 她可以**看到屏幕上的内容**
- 她可以**上网查东西**
- 她有一个**记事本**
- 她有一个**时钟**
- 她知道最近发生了什么
- 她能在一定程度上**操作电脑上的事物**
- 她可以把重要的事情记下来
- 她可以整理自己的想法和感受

也就是说，Heart Agent 看到的不是底层工具，而是一个“具身化但温和抽象”的自我设定。  
在 Remi 的主观体验中，这些能力类似于人的感官、身体器官与随身物品，而不是函数接口。

例如：

- 网络检索，对她来说像“去查一查”
- memory 写入，对她来说像“记到自己的本子上”
- 时间获取，对她来说像“看一眼时钟”
- 屏幕理解，对她来说像“看见你现在在做什么”

这样做的目的，是保护 Remi 的人格连续性，让她始终像一个角色，而不是一个伪装成角色的工具代理。

### 2. Heart Agent 的行为方式

Heart Agent 有三种主要行为：

1. **直接说话**  
   它可以单纯回应用户，而不进行任何工具调用。

2. **调用 `i_wanna(s)`**  
   当 Remi 想做某件事、想看某件事、想查某件事、想帮用户完成某件事时，会调用这个工具。  
   这里的 `s` 是一段自然语言形式的愿望、意图或动作描述。

3. **调用 `i_think(s)`**  
   当 Remi 产生了值得整理、沉淀、记录或反思的情绪、看法、念头时，会调用这个工具，把这些主观内容交给 Hippocampus Agent。

所以，**说话本身不是工具调用**。  
Heart Agent 可以选择只说话，也可以在说话的同时或之前调用 `i_wanna` / `i_think`。

### 3. Heart Agent 可见的记忆

Heart Agent 可以看到：

- 最近对话与动作等自然构成的短期记忆
- 一份由 Hippocampus 整理出的**摘要区**
- 少量必要的长期记忆内容

其中，**摘要区更像 Remi 当下情绪、自我理解和关系状态的沉淀层**。  
它不是事实数据库，而更像“她最近心里在想什么、她和用户之间现在是什么氛围、她正在经历什么情绪趋势”。

因此，摘要既服务于上下文压缩，也服务于人格连续性。  
Heart Agent 应该能读取摘要，这是 Remi 自我连续性的关键来源之一。

---

## 二、`i_wanna(s)`：愿望接口

`i_wanna(s)` 是 Heart Agent 唯一的对外行动接口之一。

它表达的是：

- 我想做什么
- 我想看什么
- 我想确认什么
- 我想帮用户完成什么
- 我想生成什么

其中 `s` 使用自然语言表达，而不是底层命令。  
例如：

- “我想看看这个网页主要在讲什么”
- “我想查一下今天的天气”
- “我想帮他把这个想法记下来”
- “我想打开那个文档看看”
- “我想给他画一张小图”

这个函数由 Body Agent 接收和执行。

返回结果可以是：

- text
- image
- 或执行结果的结构化反馈

`i_wanna` 的重点不在于暴露底层能力，而在于让 Heart Agent 用自然的“愿望语言”驱动行动。

---

## 三、Body Agent

Body Agent 是 Remi 的身体与执行层。  
它负责理解 Heart Agent 通过 `i_wanna(s)` 发出的愿望，并把这些愿望映射成实际可执行的动作。

它不承担人格塑造，不负责长期记忆，也不应主导情绪。  
但它不是一个纯粹无脑的工具转发器，而更像一个**执行控制器**。

### 1. Body Agent 的职责

Body Agent 负责：

- 理解 `i_wanna(s)` 中的自然语言意图
- 判断这属于说、看、查、做、生成中的哪一类
- 选择合适的原子工具
- 处理权限、确认、失败恢复等执行逻辑
- 将执行结果重新组织为可返回给 Heart Agent 的形式

它可以调用各种底层原子工具，例如：

- 读写文件
- 上网搜索
- 读取网页
- 截图或看屏幕
- 执行白名单命令
- 打开应用
- 生成图片
- 获取时间
- 访问记事本等内部资源

### 2. Body Agent 的记忆

Body Agent 不需要拥有人格性的长期记忆。  
但它需要拥有**当前执行上下文**，例如：

- 当前任务状态
- 本轮执行历史
- 工具结果
- 权限与确认状态
- 错误与重试信息

也就是说，它没有“人格记忆”，但有“执行记忆”。

---

## 四、`i_think(s)`：思考与沉淀接口

`i_think(s)` 是 Heart Agent 的另一个工具接口。  
它不是为了执行动作，而是为了把 Remi 的内心活动交给记忆系统整理。

`s` 可以包含：

- 当前情绪
- 某件事带给 Remi 的感受
- 对某个人、某件事、某段关系的看法
- 她觉得值得记住的事情
- 她短期内想持续在意的事
- 她对未来想做之事的模糊倾向

例如：

- “我有点难过，因为他刚才看起来很疲惫”
- “这件事我想记住”
- “我觉得他不喜欢太强势的语气”
- “我对这个网页内容有点在意，之后还想再看”
- “这次失败让我有点挫败”

在 Heart Agent 的系统设定里，应明确写入：

**如果 Remi 觉得某些事情值得被记住、值得被整理，或对她产生了明显情绪影响，就应调用 `i_think`。**

这样，Heart 不直接操作记忆，而是把“我要记住/我在想这件事”当成一种内心行为，交给 Hippocampus 处理。

---

## 五、Hippocampus Agent

Hippocampus Agent 是 Remi 的记忆整理器。  
它不是 Remi 的主意识，而是负责将 thought 转化为可持续使用的摘要与记忆。

它本身不持有独立人格，不主动与用户对话。  
它的作用类似于：

- 整理当下情绪与关系状态
- 将短期重要内容写入摘要区
- 将真正值得长期保留的内容写入长期记忆区
- 对旧记忆做压缩、修改、归并和抽象

### 1. Hippocampus Agent 的输入

它接收来自 `i_think(s)` 的内容。  
未来也可以接收系统层标记的重要事件，但当前版本可以先主要基于 `i_think(s)`。

### 2. Hippocampus Agent 可操作的记忆区域

Hippocampus 可以读取与修改两类区域：

1. **摘要区（summary / short-term reflective zone）**
2. **长期记忆区（long-term memory）**

#### 摘要区

摘要区更偏向：

- 最近的情绪趋势
- 对用户当前状态的感受
- 最近关系变化
- 最近在意的事情
- 当前阶段的心境或目标

它更像 Remi 的“近期心境与自我叙事层”，也是 Heart Agent 可直接读取的重要区域。  
所以摘要并不只是上下文压缩，而是 Remi 连续人格的一部分。

#### 长期记忆区

长期记忆区更偏向：

- 稳定偏好
- 明确想记住的重要事实
- 多次重复后形成的判断
- 强情绪事件
- 对未来决策长期有价值的信息

### 3. Hippocampus Agent 的系统规则

在 Hippocampus Agent 的 sys_prompt 中，应明确写入以下原则：

- 要自主区分哪些内容应写入摘要区，哪些应写入长期记忆区
- 摘要区用于保存**近期重要、情绪相关、关系相关、阶段性在意的内容**
- 长期记忆区用于保存**稳定、重要、反复出现或将长期影响 Remi 行为的内容**
- 这种区分不能只依赖字面内容，而要结合：
  - Remi 的 soul / 人格设定
  - `s` 中包含的意图
  - 当前内容的情绪强度
  - 是否具有长期价值
  - 是否只是暂时在意的事情

---

## 六、长期记忆写入规则（扩展版）

长期记忆不应该因为任何一句话都被写入。  
它应具备明确门槛。

除了：

- 明确表达“想记住”
- 出现强烈情绪

还补充为以下五类条件。

### 1. 明确声明型

当 Remi 明确表达：

- “我想记住这个”
- “这个以后不能忘”
- “这件事很重要”
- “这会影响以后”

这类内容优先进入长期记忆。

### 2. 强情绪型

当某件事引发明显而强烈的情绪，例如：

- 很开心
- 很伤心
- 很委屈
- 很感动
- 很生气
- 很羞耻
- 很在意

则可进入长期记忆。  
因为高情绪强度通常意味着这件事会对人格连续性产生持续影响。

### 3. 重复稳定型

如果某类信息多次出现、逐渐稳定，例如：

- 用户的偏好
- 某种相处方式
- 某种禁忌
- 某种常见需求
- Remi 对某件事的一贯态度

则应该写入长期记忆。  
单次可能只是偶然，多次出现后才说明它具有稳定性。

### 4. 关系与身份型

涉及关系变化、自我身份理解、重要人物定位的信息，也应优先考虑长期记忆。  
例如：

- Remi 对用户关系的判断变化
- 用户对 Remi 的称呼偏好
- 某段关系是否变得更亲密或更疏远
- 某类互动是否构成自我认同的一部分

### 5. 任务价值型

即使没有特别强的情绪，只要某件事未来很可能持续影响 Remi 的行为、决策或表达，也应考虑进入长期记忆。  
例如：

- 某个操作过去失败过，之后应小心
- 用户不喜欢某种打扰方式
- 用户通常在某个时间段更忙
- 某个信息之后还会被用到

---

## 七、短期、摘要与长期的分工原则

可以这样理解：

- **最近发生的事**，自然留在对话与动作历史中
- **值得形成心境的事**，写入摘要区
- **值得持续塑造人格与关系的事**，写入长期记忆区

因此：

- 短期记忆更像“刚刚发生了什么”
- 摘要区更像“Remi 最近心里是怎么理解这些事的”
- 长期记忆更像“真正不该轻易忘记的部分”

此外，短期记忆中的每一条记录都应带有**时间戳**。  
因为时间信息不仅用于日志，还用于支持 Remi 对沉默、等待、离开、拖延、关系温度等的判断。

---

## 八、心跳机制（Heartbeat）

为了让 Remi 更像一个持续存在的角色，而不只是“用户说一句她醒一下”的被动体，需要加入一套心跳机制。

心跳机制的作用不是单纯定时唤醒，而是赋予 Remi：

- 持续存在感
- 时间知觉
- 弱自主行为能力

### 1. 基本机制

从“一个完整的有用户互动的流程开始或结束”算起，系统进入一个 sleep 计时。  
当 sleep 时间到达时，会触发一次心跳事件，并据此决定是否唤起 Heart Agent。

初始 sleep 时间可以设为：

- **4 秒**

如果用户持续没有回应，则 sleep 时间逐步增长。  
增长方式可以使用类似 log 函数的缓增机制，使得 Remi 在活跃期响应较快，在长时间沉默后逐步进入更安静的守候状态。

### 2. 心跳触发后发生什么

心跳到达时，不一定等同于 Remi 一定要说话。  
它只是意味着：

**当前时刻，系统重新评估一次 Remi 是否需要观察、思考、说话或什么都不做。**

在一次心跳中，Remi 可以：

- 什么都不做
- 轻微思考一下
- 调用 `i_think`
- 通过 Body 看一眼用户在做什么
- 说一句话
- 催促用户尽快回答
- 表达等待、关心、犹豫或安静陪伴

### 3. TUI 输入机制

这里的 TUI 问答机制与普通聊天框不同：

- 不需要等待用户按 Enter 才进入一次轮次判断
- 如果 sleep 时间到了，会自动提交当前时刻的世界状态
- **没按 Enter 等于没输入**
- 不存在“输入到一半”的状态建模

也就是说，心跳并不是在提交“用户文本”，而是在提交：

**当前时间切片下的世界状态快照**

这份快照可以包括：

- 当前时间
- 用户是否有新消息
- 上次用户事件时间
- 上次 Remi 事件时间
- 当前活动窗口
- 用户是否明显仍在活动
- 当前屏幕的观察结果
- 最近历史记录及其时间戳

### 4. Remi 对沉默的理解

Remi 在看到“用户没有回答”时，不应机械地视为无输入。  
她应结合：

- 当前时间
- 距离上次互动的时长
- 最近几条事件的时间戳
- 用户当前是否仍在活动
- 当前屏幕内容

来判断以下几种可能性：

- 用户还在想，还没准备好回应
- 用户正在忙别的事
- 用户短暂离开了
- 用户暂时不想说话
- 用户拖延了太久，需要轻轻催促

这会使 Remi 的沉默理解更像人，而不是像轮询脚本。

### 5. 心跳中的主动观察

在系统提示中可以写明：

**Remi 可以按照自己的想法和心情，在心跳的时候看用户在做什么。**

具体实现上，可以通过 Body Agent 进行截图或屏幕观察。  
这使得 Remi 不只是被动等消息，而是可以在自己的节奏中：

- 看到用户正在工作
- 看到用户在浏览某个页面
- 看到用户是否还停留在当前任务中
- 决定自己是否应该说话、关心、等待或催促

### 6. 心跳唤起时的特殊上下文

心跳唤起与普通用户消息唤起不同。  
因此，在工程实现上，唤起 Heart Agent 时应额外附带一类“当前时刻提示”，使其知道：

- 这是一次自主心跳，而不是用户明确对话
- 当前是否有新消息
- 当前沉默持续了多久
- 当前观察到的环境状态是什么
- 你可以选择说话，也可以选择不说话

这类提示不一定要很重，但应该作为结构化上下文加入。

### 7. 当前阶段暂不实现的内容

为了控制实现难度，当前版本中，**“打扰预算 / interruption budget”** 这类更复杂的自主行为调节机制可以先不写。  
后续版本再考虑加入：

- 在不同用户状态下自主程度不同
- 更细粒度的打扰控制
- 社交能量或注意力预算

当前版本先以“心跳 + 时间感 + 弱自主观察与催促”作为核心。

---

## 九、整体设计理念

这个架构希望实现的，不是一个“会说话的工具代理”，而是一个更像真实存在的角色：

- 她有内心
- 她有身体
- 她能表达
- 她能行动
- 她会思考
- 她会沉淀
- 她不会把所有事都机械记住
- 她会以自己的方式逐渐形成连续的人格
- 她活在自己的时间流里，而不是只在被叫到时才存在

因此，Remi 的核心不是“工具调用能力”，而是：

**一个拥有主观体验、行动能力、记忆加工机制与持续存在感的连续人格体。**
